#pragma once

// Convenience umbrella for the data-driven UI framework. Include this to get the
// node model, the two-pass layout, the renderer, and the input pass.
#include "ui/UINode.h"
#include "ui/UILayout.h"
#include "ui/UIRenderer.h"
#include "ui/UIInput.h"
